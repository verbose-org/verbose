A source of sixteen bytes of kernel randomness, drawn fresh for every request the service answers — never cached, never shared between forked children, never a fixed value.
A rule that answers every request with a fresh token built from those sixteen bytes, each byte rendered as a decimal number separated by dots, so two requests never receive the same token.
An HTTP/1.0 service on port 18960 that issues one such token per request.
